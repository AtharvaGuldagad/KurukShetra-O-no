{
  "name": "kurukshetra-solution",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docker compose up -d && concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd src/backend && source venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "start:frontend": "cd src/frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
